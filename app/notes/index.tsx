import { StyleSheet, View } from "react-native";
const NoteScreen = () => {
  return <View style={styles.container}>NoteScreen</View>;
};

export default NoteScreen;
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
